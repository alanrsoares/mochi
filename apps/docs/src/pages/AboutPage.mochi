// About page shell — same MPA chrome as PlaygroundPage (ADR 0008), story body
// in AboutScrolly.
import { ColorSchemeToggle, Icon } from "../host/widgets.host"
import { AboutScrolly } from "../components/AboutScrolly"
import { NavLink } from "../ui/primitives"

export let AboutPage = props =>
  <div className="site-grain flex min-h-screen flex-col bg-paper text-ink">
    <header
      className="sticky top-0 z-50 border-line border-b-2 bg-paper/90 backdrop-blur-md"
    >
      <div
        className="mx-auto flex h-14 max-w-6xl items-center justify-between px-6"
      >
        <a
          href={props.homeHref}
          className="flex items-center gap-3 no-underline"
        >
          <img
            src={props.logo}
            alt=""
            className="h-8 w-8 rounded-full object-contain"
          />
          <span
            className="font-bold font-display text-ink text-lg tracking-tight"
          >
            {"mochi"}
          </span>
        </a>
        <nav
          className="flex items-center gap-3 font-mono text-mute text-xs md:gap-5"
        >
          <NavLink href={props.homeHref}>{"docs"}</NavLink>
          <NavLink href={props.playgroundHref}>{"playground"}</NavLink>
          <span
            className="border-fur border-b-2 pb-0.5 font-semibold text-fur-deep"
          >
            {"about"}
          </span>
          <ColorSchemeToggle />
          <a
            href="https://github.com/alanrsoares/mochi"
            target="_blank"
            rel="noreferrer"
            className="inline-flex items-center gap-1.5 transition-colors hover:text-fur-deep"
          >
            <Icon name="github" className="size-3.5 shrink-0" />
            <span>{"GitHub"}</span>
          </a>
        </nav>
      </div>
    </header>
    <main className="mx-auto w-full max-w-6xl flex-1 px-6 py-14 lg:py-20">
      <div className="max-w-xl">
        <div
          className="font-bold font-mono text-2xs text-fur-deep uppercase tracking-eyebrow"
        >
          {"about"}
        </div>
        <h1
          className="mt-2 font-display font-extrabold text-4xl text-ink tracking-tight"
        >
          {"A small language, looking back"}
        </h1>
        <p className="mt-3 text-copy text-mute leading-relaxed">
          {"mochi wasn't a master plan. It was one question, answered a step at a time. This is the story as I can tell it in hindsight."}
        </p>
      </div>
      <div className="mt-12 lg:mt-16"><AboutScrolly /></div>
    </main>
    <footer className="border-line border-t-2 bg-peach py-8">
      <div
        className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-3 px-6 font-mono text-mute text-xs sm:flex-row"
      >
        <div className="flex items-center gap-3">
          <img
            src={props.logo}
            alt=""
            className="h-6 w-6 rounded-full object-contain"
          />
          <span>{"mochi"}</span>
        </div>
        <NavLink
          href="https://github.com/alanrsoares/mochi/blob/main/docs/adr"
          target="_blank"
          rel="noreferrer"
        >
          {"decisions live in docs/adr"}
        </NavLink>
      </div>
    </footer>
  </div>
