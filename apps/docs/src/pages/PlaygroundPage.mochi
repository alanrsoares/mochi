// Full-viewport playground shell — ReScript try IA, mochi chrome.
// Playground stays TSX (compile / hooks / imperative preview host).
import { Playground } from "../host/widgets.host"

let PlaygroundPage = props =>
  <div
    className="site-grain flex h-dvh flex-col overflow-hidden bg-paper text-ink"
  >
    <header
      className="z-50 shrink-0 border-line border-b-2 bg-paper/90 backdrop-blur-md"
    >
      <div
        className="flex h-12 items-center justify-between gap-4 px-4 sm:px-6"
      >
        <a
          href={props.homeHref}
          className="flex items-center gap-2.5 no-underline"
        >
          <img
            src={props.logo}
            alt=""
            className="h-7 w-7 rounded-full object-contain"
          />
          <span
            className="font-bold font-display text-base text-ink tracking-tight"
          >
            {"mochi"}
          </span>
        </a>
        <nav className="flex items-center gap-4 font-mono text-mute text-xs">
          <a
            href={props.homeHref}
            className="transition-colors hover:text-fur-deep"
          >
            {"Docs"}
          </a>
          <span
            className="border-fur border-b-2 pb-0.5 font-semibold text-fur-deep"
          >
            {"Playground"}
          </span>
          <a
            href="https://github.com/alanrsoares/mochi"
            target="_blank"
            rel="noreferrer"
            className="transition-colors hover:text-fur-deep"
          >
            {"GitHub ↗"}
          </a>
        </nav>
      </div>
    </header>
    <main className="flex min-h-0 flex-1 flex-col"><Playground /></main>
  </div>
