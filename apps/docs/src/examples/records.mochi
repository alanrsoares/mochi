// formatUser needs name + role. Anything with them fits — id rides along.

let formatUser = user =>
  //  ^?
  user.name ++ " (" ++ user.role ++ ")"

let admin = { name: "Alan", role: "Maintainer", id: 42 }
let formatted = formatUser(admin)
//  ^?
