import { match } from "@onrails/pattern";
// …prelude helpers (_curry, mul, square, pi)…

const Circle = (_0) => ({ _tag: "Circle", _0 });
const Rect = _curry(2, (_0, _1) => ({ _tag: "Rect", _0, _1 }));
const area = (shape) => match(shape)
  .with({ _tag: "Circle" }, ({ _0: r }) => mul(pi, square(r)))
  .with({ _tag: "Rect" }, ({ _0: w, _1: h }) => mul(w, h))
  .exhaustive();
