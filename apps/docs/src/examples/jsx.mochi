// JSX is built in. It compiles to whatever h() your host gives you.

let Button = props =>
  //  ^?
  <button className={props.kind} disabled={props.disabled}>
    {props.label}
  </button>

let app = <Button kind="primary" label="Click me" disabled={false} />
//  ^?
