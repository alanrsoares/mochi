// Composes styled-cva shells from primitives.mochi (ADR 0009).
import { BadgeShell, BadgePulse } from "../ui/primitives"

let HeaderBadge = props =>
  <BadgeShell $tone="rose">
    <BadgePulse $tone="rose" />
    <span>{props.label}</span>
  </BadgeShell>
