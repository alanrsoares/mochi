/// <reference types="vite/client" />

// `.mochi` modules are typed per-module by generated `*.d.mochi.ts` sidecars
// (`bun run gen:mochi-dts` / the `dev`/`build`/`check` scripts), not by a
// wildcard ambient declaration — see apps/docs/src/assets.d.ts.
