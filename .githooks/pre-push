#!/usr/bin/env bash
# Local QA gate — same command as CI (.github/workflows/ci.yml).
# Escape hatch (rare): git push --no-verify
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
exec bun run check:full
